:root {
  --grid-gutter: var(--space_xl);
  --grid-gutter_l: var(--space_l);

  --grid-margin: var(--space_l);
}

/* --- General ---------- */
.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
}


/* --- Home & Navbar ---------- */
.home--wrapper,
.navbar--wrapper {
  position: relative;

  max-width: 1296px;
  padding-left: var(--grid-margin);
  padding-right: var(--grid-margin);
  margin: auto;
}
